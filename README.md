# STILL HERE — $HERE

The first website for the HEREVERSE.

## Included

- Responsive cinematic landing page
- Working mobile navigation
- Social-link placeholders
- SEO metadata
- Favicon
- `robots.txt` and `sitemap.xml`
- Custom `404.html`
- GitHub Actions deployment workflow
- Final GitHub Pages deployment checklist

## GitHub Pages

Use **Settings → Pages → Build and deployment → GitHub Actions**.

The workflow at `.github/workflows/pages.yml` deploys the repository root automatically whenever `main` is updated.

See `DEPLOYMENT-CHECKLIST.md` before launch.

## Before launch

Replace placeholder social URLs, add the final HERE pug artwork, add the final social preview image, and update any domain-specific SEO URLs.

The `$HERE` token section intentionally remains **COMING SOON** until the project is ready.
